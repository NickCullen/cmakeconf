cmake_minimum_required (VERSION 2.6)
project(My Example Project)
set_property(GLOBAL PROPERTY USE_FOLDERS ON)
add_definitions(-DMySharedDef)
add_definitions(-DAnotherDef)
if(Mac)
	add_definitions(-DMyMacDef)
endif()
if(Lin)
	add_definitions(-DMyLinuxDef)
endif()
if(Win)
	add_definitions(-DMyWindowsDef)
endif()
